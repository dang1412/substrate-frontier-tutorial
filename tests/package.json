{
  "name": "frontier-ts-test",
  "version": "0.0.0",
  "description": "frontier tutorial tests",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/node": "^18.15.11",
    "typescript": "^5.0.3"
  },
  "dependencies": {
    "ethers": "^6.2.3"
  }
}
